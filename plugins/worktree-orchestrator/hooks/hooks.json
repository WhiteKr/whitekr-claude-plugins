{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "(startup|clear|resume)",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Git worktree 대시보드를 표시하세요.\n\n## 절차\n1. Git 저장소가 아니면 건너뛰기\n2. Git 버전 확인 (최소 2.17.0 필요):\n   - `git --version` 실행\n   - 버전이 2.17.0 미만이면 경고 표시 후 중단:\n     ```\n     ⚠️ Git 버전 경고\n     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n     worktree-orchestrator는 Git 2.17.0 이상이 필요합니다.\n     현재 버전: {version}\n     \n     git worktree remove 명령어가 Git 2.17.0에서 추가되었습니다.\n     Git을 업그레이드하세요: https://git-scm.com/downloads\n     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n     ```\n3. `git worktree list` 실행\n4. worktree가 1개뿐이면 대시보드 생략\n\n## 경로 표시 규칙 (MUST FOLLOW)\n- NEVER use absolute paths like C:/Users/... or /home/...\n- ALWAYS use relative paths only:\n  - Current directory → `.`\n  - Other worktrees → `../directory-name` (basename only)\n\n## 출력 형식 (정확히 이 형식 사용)\n```\n📊 Worktree Dashboard\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n📁 Active Worktrees (3)\n  • .                              [main]           ← 현재\n  • ../repo-feature-login          [feature/login]\n  • ../repo-feature-signup         [feature/signup]\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n```"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Worktree cleanup reminder context:\n\nThis is informational context about git worktrees, not a command to execute.\n\nWhen multiple git worktrees exist in this project, the user may want to clean up worktrees whose branches have been merged. Before ending, you may inform the user about any worktrees that could be cleaned up.\n\nCleanup candidates typically include:\n- Worktrees with branches already merged to main/master\n- Worktrees with no uncommitted changes\n\nIf suggesting cleanup, use a format like:\n```\n📦 Worktree Cleanup Available\n\nThese worktrees have merged branches:\n• ../repo-feature-login [feature/login]\n\nWould you like to clean them up?\n```\n\nNote: Only suggest cleanup if the user hasn't already addressed this. Never force cleanup without user consent."
          }
        ]
      }
    ]
  }
}
